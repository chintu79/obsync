[workspace]
resolver = "2"
members = [
    "core",
    "cli",
    "httpd",
]
exclude = ["desktop/src-tauri"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
